// Page Horses & Racing Caraibes CANONIQUE — reutilise le rendu de caribbean-v2
// (cards facon regatta, bouton sous-region, sans bande navy). L'ancienne version
// reste disponible sous caribbean-v1 pour comparaison interne.
export { default, metadata } from '../caribbean-v2/page';
